{
  "name": "meta-dotfiles",
  "module": "src/main.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "scripts": {
    "dev": "bun test --watch --coverage",
    "compile": "bun build ./src/main.ts --compile --outfile build/bin/mf",
    "test": "bun test --coverage",
    "lint": "tsc --noEmit --noUnusedLocals --noUnusedParameters --pretty"
  },
  "peerDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@effect/cli": "^0.28.9",
    "@effect/platform-bun": "^0.28.4",
    "common-path-prefix": "^3.0.0",
    "effect": "^2.2.1",
    "xstate": "^5.5.2"
  }
}

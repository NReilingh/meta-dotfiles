{
  "name": "meta-dotfiles",
  "module": "src/main.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "scripts": {
    "dev": "bun test --watch",
    "compile": "bun build ./src/main.ts --outfile build/bin/mf",
    "test": "bun test --coverage"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "common-path-prefix": "^3.0.0",
    "effect": "^2.0.0-next.55"
  }
}
